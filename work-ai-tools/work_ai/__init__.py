"""work-ai-tools — standalone work automation with local LLM integration.

Read-only by default. The ONLY permitted mutation across all integrations
is setting a GitHub Project iteration field (sprint rollover), and that
requires a cryptographic confirmation token produced by the UI.

Monday.com is PERMANENTLY read-only — no write methods exist on the client.
"""
